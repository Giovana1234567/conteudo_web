/**
 * CONFIGURAÇÃO GLOBAL DE PORTA E URL DO BACKEND
 * 
 * Para alterar a porta ou o endereço de conexão com a API do NestJS durante a prova:
 * 1. Modifique a constante API_BASE_URL abaixo.
 * 2. Certifique-se de que a porta seja a mesma na qual o NestJS está rodando (ex: http://localhost:3007).
 */
export const API_BASE_URL = 'http://localhost:3007';
